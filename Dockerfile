# Extend FreeCAD Server for additional customization (optional)
FROM lscr.io/linuxserver/freecad:latest

# Install custom packages if needed
# ENV INSTALL_PACKAGES="libfuse2 git gdb"
# ENV DOCKER_MODS="linuxserver/mods:universal-package-install"

# You can add more configuration here
